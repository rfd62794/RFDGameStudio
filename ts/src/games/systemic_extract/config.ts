import type { GameConfig } from '../../engine/types';

const config: GameConfig = {
  gameId: 'systemic_extract',
  label: 'Systemic Extract',
  description: 'A 2D top-down systemic extraction sandbox: ECS-driven raids with spreading hazards and escalating hives, plus a hideout loop of research, deconstruction, and fabrication.',
  color: '#22d3ee',
  status: 'external',
  genre: 'roguelike',
  tags: ['extraction', 'ecs-sandbox', 'base-building'],
  embedUrl: '/arcade/systemic_extract/',
};

export default config;
