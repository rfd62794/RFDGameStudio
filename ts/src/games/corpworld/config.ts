import type { GameConfig } from '../../engine/types';

const config: GameConfig = {
  gameId: 'corpworld',
  label: 'CorpWorld',
  description: 'A cold-corporate land-grab on a newly-discovered planet — Voronoi-tessellated territory, symmetric fog-of-war, deterministic Circle/Square/Triangle combat. v0.1.0R4: all known bugs fixed and verified against source. Boardroom dashboard redesign — nested cadence display, an action queue for unresolved items, live garrison indicators on the map, and a real XCOM-style mode shift into monthly combat.',
  color: '#f59e0b',
  status: 'external',
  embedUrl: '/arcade/corpworld/',
};

export default config;
