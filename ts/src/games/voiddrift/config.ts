import type { GameConfig } from '../../engine/types';

const config: GameConfig = {
  gameId: 'voiddrift',
  label: 'VoidDrift',
  description: 'Survive the void — dodge, drift, and outlast waves of procedural hazards.',
  color: '#6366f1',
  status: 'external',
  externalUrl: 'https://rdug627.itch.io/voidrift',
};

export default config;
